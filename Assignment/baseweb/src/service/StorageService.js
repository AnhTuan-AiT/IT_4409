// import fs from "fs";
// import { rootPath } from "../../app.js";

// export class StorageService {
//   createFolder = (relPath2folder) => {
//     const path = rootPath + relPath2folder;

//     if (!fs.existsSync(path)) {
//       fs.mkdirSync(path, { recursive: true });
//     }
//   };
// }
